package views;

import javax.swing.*;
import java.awt.*;

public class LoginView extends JFrame {
    public LoginView() {
        setTitle("Frama - Welcome");
        JPanel mainPanel = new JPanel();

        // ...Code here

        add(mainPanel);
        setVisible(true);
    }
}
